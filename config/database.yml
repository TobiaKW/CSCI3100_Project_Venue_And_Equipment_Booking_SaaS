default: &default
  adapter: postgresql
  encoding: unicode
  host: localhost
  username: kevin    
  password:
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: proj_development

test:
  <<: *default
  database: proj_test

production:
  <<: *default
  database: proj_production
